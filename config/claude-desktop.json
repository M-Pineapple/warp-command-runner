{
  "_comment": "Claude Desktop — ~/Library/Application Support/Claude/claude_desktop_config.json. Point at the binary INSIDE the .app bundle so macOS can prompt for TCC on the keystroke-routing tools.",
  "mcpServers": {
    "warp-command-runner": {
      "command": "/Applications/Warp Command Runner.app/Contents/MacOS/warp-command-runner",
      "args": []
    }
  }
}
