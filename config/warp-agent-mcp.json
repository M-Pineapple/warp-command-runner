{
  "_comment": "Warp Agent — ~/.warp/.mcp.json. Warp's agent uses whichever LLM you configured (Grok, Claude, GPT, Gemini, …). Merge into an existing mcpServers object if you already have one.",
  "mcpServers": {
    "warp-command-runner": {
      "command": "/Applications/Warp Command Runner.app/Contents/MacOS/warp-command-runner",
      "args": []
    }
  }
}
